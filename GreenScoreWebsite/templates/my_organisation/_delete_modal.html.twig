<div
        data-delete-organisation-target="modal"
        class="fixed inset-0 bg-black bg-opacity-50 items-center justify-center hidden"
        style="z-index: 1000;"
>
    <div class="bg-white rounded-lg p-6 max-w-md mx-auto mt-20">
        <h2 class="text-xl font-semibold mb-4">Confirmer la suppression</h2>
        <p class="text-gray-600 mb-6">Êtes-vous sûr de vouloir supprimer votre organisation ? Cette action est irréversible et supprimera tous les membres associés.</p>
        <div class="flex justify-end gap-4">
            <button
                    data-action="click->delete-organisation#closeModal"
                    class="px-4 py-2 text-gray-600 hover:bg-gray-100 rounded-lg"
            >
                Annuler
            </button>
            <button
                    data-action="click->delete-organisation#handleDelete"
                    class="px-4 py-2 bg-red-600 text-white rounded-lg hover:bg-red-700"
            >
                Supprimer
            </button>
        </div>
    </div>
</div>